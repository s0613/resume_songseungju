# QA Verdict — 블로그 실시스템화 (조회수·댓글·Supabase) + 인사이트 이관
> 생성일: 2026-07-27 · run:20260727-081336-49493 (2차 사이클) · 실 Supabase + 프로덕션 빌드(next start) 대상 독립 검증

## 테스트 케이스 (pm-brief 완료 조건 1:1 대조)
- [x] `npm run build` exit 0 (보안 수정 반영 후 재빌드)
- [x] /s-skills/insights 라우트 부재 + next.config redirects 2건(목록·상세, 308 영구) — 구 URL → /blog/s-skills-in-one 리다이렉트 실동작 확인
- [x] blog.ts에 "S-Skills" 카테고리 + 이관 포스트(s-skills-in-one), /blog 프리렌더에 카테고리 노출, SSG 페이지 생성
- [x] 댓글 시나리오 (실 Supabase): 생성 201 → 목록 포함 → 오답 삭제 403 → 정답 삭제 200
- [x] 조회수 POST 2회 → count 정확히 +1씩 증가
- [x] 댓글 GET 응답에 password_hash 0건
- [x] 키 하드코딩 0건, .env.local git 미추적, supabase/.temp gitignore 적용
- [x] 소스 내 /s-skills/insights 참조 0건 (redirect 정의 제외)
- [x] (추가) JSON null 바디 400, 73바이트 비밀번호 400, honeypot 204, 미존재 slug 400 — Codex 지적 수정분 재검증
- [x] 테스트 데이터 전량 정리 (댓글 삭제·조회수 행 삭제 204)

## 엣지 케이스
- 프로덕션(EC2)에 SUPABASE env 미설정 시 → API 503, 프론트는 댓글·조회수 섹션 미표시(페이지 정상) — 코드 경로 존재, 실서버 검증은 배포 후
- StrictMode 이중 마운트 → 조회수 delta 1 확인 (frontend Playwright 검증)
- bcrypt CPU 소진·삭제 브루트포스 — 수용 리스크, nginx rate limit 후속 권장

## 판정: PASS
완료 조건 8/8 + Codex 수정분 4건 전부 실행·관찰로 충족. 실 Supabase 대상 10개 시나리오 통과.

## 발견된 이슈
- LOW: 배포 시 EC2 서버에 SUPABASE_URL·SUPABASE_SERVICE_ROLE_KEY 환경 변수 설정 필수 — 미설정이면 댓글·조회수 비활성 상태로 서비스됨
- LOW: /api/blog/* nginx rate limit 권장 (bcrypt CPU·브루트포스 완화)
- LOW: 목록 조회수 미표시 (과설계 방지 결정 — 필요 시 배치 API 후속)
